import 'package:flutter/material.dart';
import 'package:one_life/Screens/Login/components/body.dart';

class LoginScreen extends StatelessWidget {
  // this the root of the login screen
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Body(),
    );
  }
}
