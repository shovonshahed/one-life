import 'package:flutter/material.dart';
import 'package:one_life/Screens/Welcome/components/body.dart';

// start of first or welocme page
class WelcomeScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Body(), // leads to the body of the welocme page
    );
  }
}
